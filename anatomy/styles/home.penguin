@import './inputs.skin';
@import './buttons.skin';

.home {
  padding: 20px;

  .text-field {
    @apply .text-field;
  }

  .primary-button {
    @apply .primary-button;
  }

  .secondary-button {
    @apply .secondary-button;
  }
}
